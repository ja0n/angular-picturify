var webpack = require('webpack');

module.exports = {
  entry: "./src/angular-picturify.module.js",
  devtool: "source-map",
  output: {
    path: __dirname + '/dist',
    filename: "angular-picturify.min.js"
  },
  module: {
    loaders: [
      { test: /\.js$/, loader: "transform?brfs" }
    ],
  },
  plugins: [
    new webpack.optimize.UglifyJsPlugin({minimize: true})
  ]
};
